name: Diff manifests

on: pull_request

jobs:
  diff:
    permissions:
      contents: read
      pull-requests: write
    name: diff
    uses: Andreas-Espelund/github-workflows/.github/workflows/diff-manifests.yaml@diff-to-comment
    with:
      path: env
