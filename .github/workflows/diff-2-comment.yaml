name: Manifests diff to PR comment

on: pull_request

jobs:
  diff-manifests:
    permissions:
      contents: read
      pull-requests: write
    uses: kartverket/github-workflows/.github/workflows/manifest-diff-to-pr-comment.yaml@f92dc0cd7ff0f16ede9da6403abed5da5c599004